# Steering

predicted_metric MUST be the brief's primary metric (declared under **Optimize:** in PROMPT.md). For tinymatmul3x3 use predicted_metric=n_params; for cifar-100 use predicted_metric=val_accuracy. Do NOT predict on the gate metric. Re-read the protocol doc (CLAUDE.md / AGENTS.md / GEMINI.md) in .distillate/ — the prereg-discipline section was just updated. Apply to every prereg from now on.
